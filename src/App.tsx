import './style.css';

export function App() {
  return (
    <main className="app">
      <section className="panel" aria-labelledby="app-title">
        <p className="eyebrow">WorldBuilderX</p>
        <h1 id="app-title">Project foundation ready</h1>
        <p className="copy">React and TypeScript renderer baseline is active.</p>
      </section>
    </main>
  );
}
